<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account - List</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
        }
        table, th, td {
            border: 1px solid black;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>

    <h1><%=title %></h1>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Email</th>
                <th>Name</th>
                <th>Country</th>
                <th>City</th>
                <th>Interests</th>
                <th>NOTE</th>
                <th>RECORD_DATE</th>
                <th>SERIAL_NUMBER</th>
                <th>ORDER_DATE</th>
                <th>Item List</th>
            </tr>
        </thead>
        <tbody>
            <% data.forEach(item => { %>
                <tr>
                    <td><%= item.ID %></td>
                    <td><%= item.EMAIL %></td>
                    <td><%= item.NAME %></td>
                    <td><%= item.COUNTRY %></td>
                    <td><%= item.CITY %></td>
                    <td><%= item.INTERESTS %></td>
                    <!-- 日期只顯示年月日 -->
                    <td><%= item.NOTE %></td>
                    <td><%= new Date(item.RECORD_DATE).toLocaleDateString('en-CA') %></td> 
                    <td><%= item.SERIAL_NUMBER %></td>
                    <td><%= item.ORDER_DATE ? new Date(item.ORDER_DATE).toLocaleDateString('en-CA') : '' %></td>
                    <!-- 要先確認null 直接new Date 會有個默認日期 -->
                    <td><%= item.PURCHASED_ITEMS %></td>
                </tr>
            <% }); %>
        </tbody>
    </table>
</body>
</html>
